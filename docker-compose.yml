version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "3001:3000"
    depends_on:
      - backend
